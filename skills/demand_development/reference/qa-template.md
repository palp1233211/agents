# 测试验收信息模板（Step 6）

> 开发完成后，为测试同学提供验收信息，确保测试同学可以快速验收，无需反复确认数据来源、计算逻辑、回归范围等问题。

---

## 模板结构

测试验收信息文档**必须**包含以下章节：

### 1. 功能概述

- 功能名称
- 核心功能描述
- 依赖需求（如有）

### 2. 数据来源与计算逻辑

#### 数据表清单

| 表名 | 数据库 | 作用 |
|------|--------|------|
| - | - | - |

#### 计算逻辑流程图（文字描述）

```
数据源 → 处理步骤 → 输出
```

#### 关键规则说明

列出核心业务规则（剔除规则、过滤条件、计算公式等）

### 3. 输出字段说明

#### Excel/报表字段

| 列名 | 数据来源 | 示例 |
|------|----------|------|

#### API 返回字段（如有）

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|

### 4. 执行与验证方式

#### 任务执行

- **任务名称**：`{task_name}`（xxl-job 任务名，snake_case 格式）
- **执行方式**：通过 xxl-job 任务调度平台手动触发
- **调度周期**：`{cron_expression}`（如 `0 0 11 16 * ? *` 表示每月16号11:00）

#### 验证 SQL

```sql
-- 验证数据存在
SELECT * FROM {table} WHERE {condition};

-- 验证计算结果
SELECT {fields} FROM {table} WHERE {condition};
```

#### 验证邮件/文件

- 收件人/路径
- 标题/文件名格式
- 内容检查点

### 5. 配置说明

#### 环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|

#### Apollo 配置（如有）

- 配置项名称
- 配置项作用

#### 调度配置

- 任务名
- 调度周期
- 生效国家

### 6. 测试场景清单

| 场景 | 验证点 | SQL 验证 |
|------|--------|----------|
| 正常场景 | - | - |
| 边界场景 | - | - |
| 异常场景 | - | - |

### 7. 其他国家回归验证

#### 需要回归的国家

列出需要回归的国家代码（如仅 TH，或 ALL）

#### 无需回归的原因

说明其他国家不受影响的原因（如：任务仅在某国家模块下、配置为国家特有等）

#### 未来关注点

其他国家如引入类似功能需注意的事项

### 8. 注意事项

- 依赖关系
- 配置生效顺序
- 数据时效
- 临时文件清理

### 9. 快速验收 Checklist

- [ ] 前置条件验证
- [ ] 配置正确
- [ ] 执行命令验证
- [ ] 输出验证
- [ ] 边界场景验证

### 10. 联系方式

开发人员联系方式（如有问题可联系）

---

## 文件落盘路径

```bash
workspace/docs/{需求号}-测试验收信息.md
```

---

## 每步验收确认语

> "以上测试验收信息是否完整？通过后需求开发完成。"